// check database

// const username = document.getElementById("name-of-user");
// const password = document.getElementById("password");
// const login = document.getElementById("login");

// const checkLogin = (e) => {
  // e.preventDefault();
  // if(!username.value) {
  //   username.setCustomValidity("this field shouldn't be empty");
  // } else if (!password.value){
  //   password.value = "this field shouldn't be empty";
  // }
  // alert("username is: " + username.value + "and password is " + password.value);
}

// login.addEventListener("click", checkLogin);
